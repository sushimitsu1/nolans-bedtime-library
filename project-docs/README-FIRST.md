# Nolan's Bedtime Library — Codex Handoff

Use the user's existing local Git repository as the source of truth. This package contains the project rules, production workflow, current inventory, and a master Codex prompt.

## Start here
1. Open the existing `nolans-bedtime-library` repository in Codex.
2. Copy the contents of `CODEX-MASTER-PROMPT.txt` into a new Codex task.
3. Keep this handoff folder inside the repo under `/project-docs/` or nearby.
4. Do not regenerate or rename existing artwork unless the user explicitly requests it.
5. Before every push, run the QA checklist in `QA-CHECKLIST.md`.

## Current status
- App is deployed on GitHub Pages.
- Books 1–10 exist in the project.
- Book 7, **El Toro Loco and the Big Red Jump**, was recently redesigned and should be treated as the current approved version in the user's local repository.
- The collection is parent-led bedtime reading. Do not add narration or automatic page advancement unless the user later changes direction.
