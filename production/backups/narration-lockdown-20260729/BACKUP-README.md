# Pre-lockdown narration backup

Created before narration implementation changes on 2026-07-29.

- 8 existing `narration.json` files were copied with their original repository paths.
- 9 existing files whose names contain `manifest` were copied with their original repository paths.
- `app.js`, `production/scripts/generate_narration.py`, and
  `production/scripts/validate_narration_voice.mjs` were copied because they contained
  the pre-lockdown browser and text-export behavior.
- No narration configuration file existed to copy.
- No generated narration audio file existed in the repository.

The copied files are preservation evidence only. Production validation ignores this
backup tree.
